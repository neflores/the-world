class WorldCity {
  const WorldCity({
    required this.id,
    required this.name,
    required this.latitude,
    required this.longitude,
  });
  final String id, name;
  final double latitude, longitude;
}
